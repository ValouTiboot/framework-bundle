*.lock
# the asset toolchain is frozen: a fresh resolution once duplicated jQuery (jquery-ui) and changed the theme
!assets/yarn.lock
assets/node_modules
assets/yarn-error.log
vendor/
var/
tests/App/var/
phpstan.neon
phpunit.xml
