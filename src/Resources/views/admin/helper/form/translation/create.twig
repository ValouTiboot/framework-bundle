{% extends '@DigitixFramework/admin/helper/form/create.twig' %}

{% block toolbar %}{% endblock %}
{% block content%}
	{% for domain, form in forms %}
		{% form_theme form '@DigitixFramework/admin/_partials/form_theme.twig' %}
		<div class="bg-white mb-2 content-container">
			<h5
				class="translationForm-title mb-0"
				data-bs-toggle="collapse"
				data-bs-target="#collapse{{ loop.index }}"
				aria-expanded="false"
				aria-controls="collapse{{ loop.index }}"
			>
				{{ domain }}
			</h5>
			<div class="collaspe-form collapse" id="collapse{{ loop.index }}" aria-labelledby="headingOne">
				<div class="container-form">
					<div class="dgtx-form">
						{{ form(form) }}
						{% if hasReturnLink %}
							<div class="form-return-link">
								<a class="btn btn-light" href="{{ path('dgtx_admin_entity_read', {entityName: entityName}) }}">
									{{ 'form.default.back'|trans({}, 'Admin.Form.Default') }}
								</a>
							</div>
						{% endif %}
					</div>
				</div>
			</div>
		</div>
	{% endfor %}
{% endblock %}

