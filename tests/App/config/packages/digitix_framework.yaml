# The test application runs the recipe shipped with the bundle, untouched:
# this keeps the recipe itself under test.
imports:
    - { resource: '../../../../recipe/digitix_framework.yaml' }

digitix_framework:
    front:
        home_route: test_home   # the "/" route of the test application (routes.yaml)
